name: Greetings

on: [pull_request_target, issues]

jobs:
    greeting:
        runs-on: ubuntu-latest
        permissions:
            issues: write
            pull-requests: write
        steps:
            - uses: actions/first-interaction@v1
              with:
                  repo-token: ${{ secrets.GITHUB_TOKEN }}
                  issue-message: 'Thank you for reporting this! We appreciate your contribution and will review it soon. 🙌'
                  pr-message: 'Awesome work! 🎉 Thank you for your contribution — our team will review your PR shortly 🚀'
